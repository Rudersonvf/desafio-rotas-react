import SimpleContent from "../../../components/SimpleContent";

export default function NotFound(){
    return(
        <main>
            <section className="section-container">
                <SimpleContent text='Não encontrado!'/>
            </section>
        </main>
    );
}